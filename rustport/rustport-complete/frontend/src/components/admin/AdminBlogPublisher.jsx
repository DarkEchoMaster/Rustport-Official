import {useState} from "react"; import ImageUploader from "../forms/ImageUploader"; import Button from "../ui/Button"; import {createPost} from "../../api/blogApi"; import {useNotifications} from "../../context/NotificationContext";
export default function AdminBlogPublisher(){const [files,setFiles]=useState([]);const {notify}=useNotifications();const submit=async e=>{e.preventDefault();const fd=new FormData(e.currentTarget);files.forEach(f=>fd.append("images",f));await createPost(fd);e.currentTarget.reset();setFiles([]);notify("Blog post published.");};return <section className="admin-panel"><h2>Publish a blog post</h2><form className="form-card" onSubmit={submit}><label>Title<input name="title" required/></label><label>Description<textarea name="content" rows="9" required/></label><ImageUploader label="Upload multiple images" name="images" multiple onChange={e=>setFiles([...e.target.files])}/><Button>Post blog</Button></form></section>;}
