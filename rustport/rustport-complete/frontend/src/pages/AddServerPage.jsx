import {useEffect,useState} from "react"; import {getSlotAvailability,createServer} from "../api/serverApi"; import {PREMIUM_PRICES,STANDARD_PLANS,SERVER_TAGS} from "../utils/constants"; import Button from "../components/ui/Button"; import ImageUploader from "../components/forms/ImageUploader"; import {useNotifications} from "../context/NotificationContext";
export default function AddServerPage(){const [slots,setSlots]=useState({premium:[],featured:[]});const {notify}=useNotifications();useEffect(()=>{getSlotAvailability().then(setSlots);},[]);const submit=async e=>{e.preventDefault();const r=await createServer(new FormData(e.currentTarget));if(r.checkoutUrl)location.assign(r.checkoutUrl);else notify("Server draft saved. Add Stripe keys to enable checkout.");};return <section className="container page"><div className="page-header"><span className="eyebrow">Owner onboarding</span><h1>List your Rust server</h1><p>Complete the form, choose an available slot, accept the policies, and publish through Stripe.</p></div><form className="form-card" onSubmit={submit}><div className="form-grid"><label>Server name<input name="name" required/></label><label>IP address and port<input name="address" placeholder="123.45.67.89:28015" required/></label><label>Region<select name="region"><option>US</option><option>EU</option><option>AU</option><option>SEA</option></select></label><label>Tags<select name="tags" multiple>{SERVER_TAGS.map(t=><option key={t}>{t}</option>)}</select></label></div><label>Short description<input name="shortDescription" maxLength="160" required/></label><label>Full description<textarea name="description" rows="8" required/></label><div className="upload-grid"><ImageUploader label="200 × 200 icon" name="icon"/><ImageUploader label="450 × 75 banner" name="banner"/><ImageUploader label="Gallery images" name="images" multiple/></div><h2>Premium slots</h2><div className="slot-grid">{PREMIUM_PRICES.map((price,i)=>{const s=slots.premium?.[i];return <label className={`slot-card ${s?.available===false?"is-disabled":""}`} key={i}><input type="radio" name="slot" value={`premium-${i+1}`} disabled={s?.available===false}/><strong>#{i+1} — ${price}/month</strong><span>{s?.available===false?"Unavailable":"Available"}</span></label>;})}</div><h2>Featured slots</h2><div className="slot-grid">{STANDARD_PLANS.map(p=><label className="slot-card" key={p.id}><input type="radio" name="slot" value={`featured-${p.id}`}/><strong>${p.price}</strong><span>{p.label}</span></label>)}</div><label className="checkbox"><input type="checkbox" name="age" required/> I confirm I am at least 18 years old.</label><label className="checkbox"><input type="checkbox" name="terms" required/> I accept the Terms of Service and 14-day refund policy.</label><Button>Continue to payment</Button></form></section>;}
