{
  "name": "rustport",
  "private": true,
  "version": "1.0.0",
  "workspaces": ["frontend", "api-server"],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace frontend\" \"npm run dev --workspace api-server\"",
    "build": "npm run build --workspace frontend",
    "start": "npm run start --workspace api-server"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
