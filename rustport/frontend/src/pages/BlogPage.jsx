import {useEffect,useState} from "react"; import {getPosts} from "../api/blogApi"; import BlogGrid from "../components/blog/BlogGrid"; import Pagination from "../components/ui/Pagination";
export default function BlogPage(){const [page,setPage]=useState(1);const [data,setData]=useState({posts:[],pagination:{}});useEffect(()=>{getPosts({page,limit:9}).then(setData);},[page]);return <section className="container page"><div className="page-header"><span className="eyebrow">Rust guides and news</span><h1>Rust Server Blog</h1><p>Server discovery, wipe advice, community management, and promotion tips.</p></div><BlogGrid posts={data.posts}/><Pagination page={page} pageCount={data.pagination.pageCount||1} onChange={setPage}/></section>;}
