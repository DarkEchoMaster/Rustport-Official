import {PREMIUM_PRICES,STANDARD_PLANS} from "../utils/constants"; import {formatCurrency} from "../utils/formatters";
export default function PricingPage(){return <section className="container page"><div className="page-header"><h1>Server listing pricing</h1><p>Unavailable slots are disabled before checkout and rechecked by Express before Stripe creates a session.</p></div><h2>Top five premium slots</h2><div className="pricing-grid">{PREMIUM_PRICES.map((p,i)=><article key={p}><span>Position {i+1}</span><strong>{formatCurrency(p)}</strong><small>per month</small></article>)}</div><h2>Ten featured slots</h2><div className="pricing-grid">{STANDARD_PLANS.map(p=><article key={p.id}><span>{p.label}</span><strong>{formatCurrency(p.price)}</strong><small>featured placement</small></article>)}</div><div className="list-cta"><div><span>Community boost</span><h2>Bump any server for $10</h2></div></div></section>;}
